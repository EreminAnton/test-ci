repos:
  - repo: local
    hooks:
      - id: poetry-export
        name: Export dependencies with Poetry
        entry: bash -c 'poetry export -f requirements.txt --output requirements.txt && git diff --exit-code requirements.txt || git add requirements.txt'
        language: system
        files: pyproject\.toml$
        pass_filenames: false